**Round-1 items: resolved? (one line each: RESOLVED / PARTIAL / NOT — why)**

- RESOLVED — phase split: `integration-plan.md:16-24` explicitly keeps missing `trump`/`obama` out of phase 1, and `integration-plan.md:55-56` repeats that they must not be added until phase 2. This matches the backend contract where only `old-man`, `young-woman`, `femme-fatale`, and `jfk` exist today.
- RESOLVED — pbxproj registration: `integration-plan.md:57-60` correctly says `VoiceCatalog.swift` must be added to `PBXFileReference`, the MessagesExtension group, `PBXBuildFile`, and the `voiceMixerMessages` sources build phase. The project really is explicit: file refs/group entries are listed at `voiceMixer.xcodeproj/project.pbxproj:50-68` and `voiceMixer.xcodeproj/project.pbxproj:80-95`, and the extension target compiles only the files in `D9AA9C3B2A3E63B4EBC18B0B /* Sources */` at `voiceMixer.xcodeproj/project.pbxproj:226-238`.
- RESOLVED — `.id` -> `voiceId` audit: `integration-plan.md:62-68` names the actual current bug, where `prepareClip` sends `selectedPersona.id` at `MessagesExtension/VoiceTransformView.swift:296-298`, and it correctly separates UI identity from the backend wire id. The relevant UI identity uses are also real: `VoicePersona` is `Identifiable` at `MessagesExtension/VoiceTransformView.swift:5-7`, `ForEach(VoicePersona.all)` appears at `MessagesExtension/VoiceTransformView.swift:542-557`, and equality drives selection/page dots at `MessagesExtension/VoiceTransformView.swift:553-556` and `MessagesExtension/VoiceTransformView.swift:570-573`.
- RESOLVED — typed errors: `integration-plan.md:70-75` requires replacing the current `invalidResponse` collapse in `MessagesExtension/ConvertService.swift:20-23`, and `integration-plan.md:82` requires non-2xx body mapping in live networking. That is concrete enough to prevent the current `LiveConvertService` behavior where every non-2xx is thrown as `invalidResponse` at `MessagesExtension/LiveConvertService.swift:31-34`.
- PARTIAL — live-switch verification: `integration-plan.md:93-99` says keep the override and set `useMock = false`, and `integration-plan.md:135-143` adds a verification step to assert `Config.useMock == false`, `Config.baseURL == https://voiceapi.awill.co`, and `LiveConvertService` is in use. That addresses the prior issue, but the plan should also specify where to put the assertion/log because service selection happens in `MessagesViewController` property initialization at `MessagesExtension/MessagesViewController.swift:9-10`, while `Config.useMock` is currently still `true` at `MessagesExtension/Config.swift:24-25`.

**New issues**

- The DEBUG `/voices` preflight is promoted at `integration-plan.md:100-103`, but the plan does not say where it lives or how it avoids blocking a Messages extension launch. Specific fix: add a small `VoiceCatalogPreflight` called from `MessagesViewController.viewDidLoad` after the root view is created, gated by `#if DEBUG`, with a timeout and log/assert behavior; otherwise the implementer may put networking in static catalog initialization or in `Config`, both poor places for async launch-time work.
- The plan asks for a request-builder test at `integration-plan.md:79-81`, but the current `LiveConvertService.multipartBody` is `private` at `MessagesExtension/LiveConvertService.swift:52-79` and there is no visible test target in `voiceMixer.xcodeproj/project.pbxproj:189-192`. Specific fix: add an explicit step to create or confirm a test target, or make a package/internal request builder that can be tested without exposing it to production callers.
- `integration-plan.md:93-99` says keep `API_BASE_URL` override and prefer build settings, but current `MessagesExtension/Info.plist:1-33` has no `API_BASE_URL` key even though `Config` reads it at `MessagesExtension/Config.swift:11-16`. Specific fix: add an implementation step to add `API_BASE_URL = $(API_BASE_URL)` to the extension Info.plist and define Debug/Release build settings, or explicitly decide the override is only opportunistic when manually added later.

**Remaining gaps**

- The plan still does not include an implementation detail for user-visible typed error copy. It requires typed errors at `integration-plan.md:70-75` and distinct verification at `integration-plan.md:141-142`, but `VoiceTransformViewModel` currently maps every conversion failure to `"Convert failed"` and resets to record at `MessagesExtension/VoiceTransformView.swift:286-291`. Specific fix: add a step to convert `ConvertServiceError` into status lines for 404/413/422/502/offline and preserve the recorded URL for retry on transient failures.
- The worktree hazard is real and remains a sequencing risk: `integration-plan.md:37-45` says resolve it first, and `git status --short` currently shows `?? MessagesExtension/VoiceTransformView.swift`, `?? integration-plan.md`, and tracked modifications in `MessagesExtension/MessagesViewController.swift`, `MessagesExtension/AudioRecorder.swift`, `MessagesExtension/WaveformVideoRenderer.swift`, and `voiceMixer.xcodeproj/project.pbxproj`. Do not start the integration branch from committed HEAD until these changes are committed or intentionally carried.
- The phase-1 optional DEBUG JFK tile is useful for `/impersonate`, but `integration-plan.md:26-28` should say it must use `engine: .modal` and `acceptsText: true` in the preflight expectation. Otherwise it can accidentally validate only voice existence, not modal routing.

**Verdict**: The revised plan is sound enough to implement now if the implementer treats the remaining gaps above as part of phase 1, not follow-up polish. The round-1 blockers are materially addressed: the missing server voices are gated, pbxproj registration is correctly specified against the real explicit sources phase, the `.id`/`voiceId` split is aimed at the exact bad call site, typed HTTP errors are required before live, and live-switch verification is present. I would tighten the preflight/test-target/config details before handing this to an implementation agent, but there is no longer a structural plan flaw that should block starting.
